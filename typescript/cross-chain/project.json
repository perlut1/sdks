{
  "name": "cross-chain",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "typescript/cross-chain/src",
  "projectType": "library",
  "tags": ["typescript", "sdk"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "typescript/cross-chain/dist",
        "main": "typescript/cross-chain/src/index.ts",
        "tsConfig": "typescript/cross-chain/tsconfig.lib.json",
        "assets": ["typescript/cross-chain/*.md"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "typescript/cross-chain/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["typescript/cross-chain/**/*.ts"]
      },
      "outputs": ["{options.outputFile}"]
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p typescript/cross-chain/tsconfig.lib.json"
      }
    }
  }
}